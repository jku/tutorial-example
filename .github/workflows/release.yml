name: Release workflow

on:
  workflow_dispatch:

# We need to specify permissions

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Initial echo
        run: echo "Start of release"
